#include <iostream>
using namespace std;
/*
int main(){
    const int row = 2, col =2;

    int staticArr[row][col] = {{1,2},{3,4}};

    std :: cout << "Static 2-Dimensional Array: ";
    for(int i=0; i<row; i++)
        for(int j=0; j<col; j++)
            std :: cout << staticArr[i][j] << " ";

    std :: cout << endl;

    int **dynamicArr = new int*[row];

    for(int i=0; i<row; i++)
        dynamicArr[i] = new int[col];

    int k=0;
    for(int i=0; i<row; i++)
        for(int j=0; j<col; j++)
            dynamicArr[i][j] = ++k;

    std :: cout << "Dynamic 2-Dimensional Array" << " ";
    for(int i=0; i<row; i++)
        for(int j=0; j<col; j++)
            std :: cout << dynamicArr[i][j] << " ";

    std :: cout << endl;

    for(int j=0; j<row; j++){
        delete [] dynamicArr[j];
    }
    delete [] dynamicArr;
}
*/

